---
name: /start
description: "Run the CLAUDE.md startup procedure: read invariants, theory docs, and refresh memories."
---

Run the startup procedure described in CLAUDE.md:

1. Read and familiarize yourself with `design_docs/invariants.csv` - the critical invariants governing the compiler and runtime.

2. Read `THEORY.md` - the GC theory and compiler pipeline overview.

3. Read all files in `design_docs/theory/` - the detailed theory documents for each compiler pass.

4. Update Serena memories to keep them fresh:
   - Read the existing memories (invariants_summary, compiler_pipeline, project_overview)
   - Refresh/update them if needed to keep the cache current

5. After completing these steps, provide a brief summary of what was loaded and confirm readiness.

IMPORTANT: This ensures you have full context about the ECO project's invariants, representation models, and compiler pipeline before working on any tasks.
